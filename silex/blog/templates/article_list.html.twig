{% for article in articles %}
    <h2>{{ article.title }}</h2>
    <p><span>{{ article.categoryName }}</span></p>
    <p>{{ article.header }}</p>
    
    
    <a href="{{ path('article_index', {id: article.id}) }}">Voir l'article</a>
    
{% endfor %}